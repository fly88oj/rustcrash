//! Wave-11 stub: registered ahead of the port pass; replaced wholesale.
//!
//! A quinn `crypto::ClientConfig` implementation over the engine's own
//! TLS 1.3 stack (`proto::reality::tls13`) — the seam that carries ECH
//! and JLS into QUIC-based outbounds.
